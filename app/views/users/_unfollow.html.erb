<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "已关注", class: "btn btn-warning btn-xs" %>
    <!--<span class="label label-success" >online</span>-->
<% end %>